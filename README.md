# Neovim configration
**Neovim configration for best codding exprience.**

# install neovim 
pkg inatall neovim 

- clone repo into ~/.config/
```
https://github.com/MyUserName-beep/neovim-config.git
```

- open neovim 
```
neovim
```

- It will install necasary packges
## some plugins
- lazy "Packge menager"
- treesitter "Syntax highlighting"
- telescope "Searching files" and so on...

## All commands lists
- map leader
```
 leader = space
```
```
 leader pv - netrw file tree
  
 [+] Folding keymaps
  
 `leader z` -- toggle fold
 `leader o` -- open fold
 `leader c` -- cloae fold
  
 [+] telescope finder
  
 `leader pf` -- find files
 `leader pg` -- live grep
 `leader gf` -- git files
 `leader pb` -- buffer
 `leader ph` -- help tags
````
